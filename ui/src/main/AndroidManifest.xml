<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Declared even though the app is offline-first: MapLibre's internal code paths
        assume these are present regardless of whether the app actually exercises them.
        See Story 1.1 Dev Notes / web research.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        Story 1.2: live "you are here" marker (FR-2). Both requested together so the
        system permission dialog offers the user "Precise"/"Approximate"; the app must
        work correctly under either (see AndroidLocationProvider's degraded-accuracy path).
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

</manifest>
